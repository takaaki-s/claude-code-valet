────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
❯ this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can
  wrap at word boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token this is a plain
  ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word
  boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token this is a plain ascii prompt with
  ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of
  splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so
  the terminal can wrap at word boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token
  this is a plain ascii prompt with ordinary spaces so the terminal can wrap at word boundaries instead of splitting a token this is a plain ascii prompt with ordinary spaces so the terminal can
  wrap at word boundaries instead of splitting a token t
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
