[English](CONTRIBUTING.md) | **日本語**

# コントリビューションについて

jind-ai に関心を持っていただきありがとうございます。

まず [プロジェクトの状態](README.ja.md#プロジェクトの状態) をお読みください。本プロジェクトは
個人が空き時間で開発・メンテナンスしているものであり、以下の方針はすべてその
前提に基づいています。

## 対応についての前提

- Issue / Pull Request への返信時間は保証できません。
- 返信がない場合、多くは却下ではなく単に時間が取れていない状態です。
- スコープ外として Issue / PR をクローズすることがあります。これはアイデアの
  価値ではなく、本プロジェクトの範囲についての判断です。

これらを先に明示するのは、萎縮させるためではなく誠実であるためです。

## 歓迎する貢献

- **再現手順つきのバグ報告**。最も価値のある貢献です。
- **バグ修正**。可能であればリグレッションテストを添えてください。
- **ドキュメント修正** — 誤字、リンク切れ、分かりにくい記述。
- 既存の挙動に対する **テストの追加**。

小さく焦点の絞られた PR ほどマージされやすくなります。

## 先に Issue を立ててほしいもの

以下はコードを書く前に相談してください。事前の議論なく送られた PR は、コードの
品質にかかわらずお断りすることがあります。

- **新機能** および TUI の変更。
- **新しい agent adapter**。各 adapter は本プロジェクトとは独立に変化する
  外部 CLI に追随するため、一度きりの貢献ではなく継続的なメンテナンス義務が
  発生します。維持し続ける現実的な見通しがある場合にのみ受け入れます。
- **複数パッケージにまたがるリファクタリング**。
- **依存関係の追加**。依存は意図的に小さく保っており、テストは標準ライブラリ
  のみを使用しています。

## スコープ外

- **Windows**。リリースビルドは Linux と macOS を対象としています。
- **tmux 以外のバックエンド**。tmux は差し替え可能な実装詳細ではなく、
  アーキテクチャ上の前提です。

## 開発

Go 1.26 と tmux が必要です。

```
make build       # → bin/jin
make test        # go test -v ./...
make test-race   # go test -race ./...
make fmt         # go fmt ./...
make lint        # golangci-lint run ./...
```

PR を出す前に `make fmt` / `make lint` / `make test-race` を実行してください。
CI では lint と race 有効のテストが実行され、いずれも通過する必要があります。

新規コードには `_test.go` を追加してください。テストは標準ライブラリのみを
使用しています（アサーションライブラリは使いません）。

全体像は [docs/architecture.md](docs/architecture.md) と
[docs/conventions.md](docs/conventions.md) を参照してください。

## コミットメッセージ

[Conventional Commits](https://www.conventionalcommits.org/) に従います。
リリース時の changelog はコミットメッセージから生成されるため、prefix が重要です。

`feat:` / `fix:` / `refactor:` / `docs:` / `test:` / `chore:`

## ライセンス

コントリビュートすることで、その内容が MIT License の下でライセンスされることに
同意したものとみなされます。
